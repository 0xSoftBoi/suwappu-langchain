interface:
  display_name: "Suwappu Langchain"
  short_description: "Repo-specific patterns and workflows for suwappu-langchain"
  default_prompt: "Use the suwappu-langchain repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true